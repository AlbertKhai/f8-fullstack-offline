'use client'

const BtnSearch = () => {
  const handleClickSearch = () => {}

  return (
    <button onClick={handleClickSearch} className='btn__search'>
      <i className='fa-solid fa-magnifying-glass'></i>
    </button>
  )
}

export default BtnSearch
